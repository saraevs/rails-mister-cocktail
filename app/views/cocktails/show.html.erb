<h1>
  <%= @cocktail.name %>
</h1>

<h3>
  Ingredients
</h3>

<% doses = @cocktail.doses %>
<ul>
  <% doses.each do |dose| %>
    <li>
      <%= dose.description %>
      <%= dose.ingredient.name %>
      <%= link_to '➖', doses_path(dose.id), method: :delete %>
    </li>
  <% end %>
</ul>

<%= link_to 'Add an ingredient', cocktail_dose_new_path(@cocktail) %> <br>
<%= link_to 'Back to cocktails', cocktails_path  %>
